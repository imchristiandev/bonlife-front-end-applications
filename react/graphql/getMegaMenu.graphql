query{
  menus @context(provider: "vtex.mega-menu@0.x") {
    display
    icon
    id
    name
    order
    slug
    styles
    menu {
      display
      icon
      id
      name
      order
      slug
      styles
      menu {
        display
        icon
        id
        name
        order
        slug
        styles
      }
    }
  }
}
